# General Code Style & Formatting

- Use PascalCase for React component file names (e.g., UserCard.tsx, not user-card.tsx).
- Use named exports for components.

# Project Information

- This project is a RAG-based ai app where the user can create workspaces where they can upload any number of documents, and query the RAG about it.

# Project Structure & Architecture

- Use React Router v7, which merged with Remix recently. This means it uses remix conventions, however the package is react-router.
- Use CSR for the majority of the core application, with SSR only for authentication or home/landing pages.
- Do not use react-router-dom, it is deprecated, most packages are in react-router.
- Use TypeScript where possible.

# Styling & UI

- Use Tailwind CSS for styling.
- Use Shadcn UI for components where possible.
- Remember that the way to add shadcn components is using "npx shadcn@latest add <component-name>"
- shadcn-ui is deprecated, use shadcn@latest
